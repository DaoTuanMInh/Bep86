# MongoDB Atlas Connection String
# Lấy từ: MongoDB Atlas -> Connect -> Drivers -> Copy connection string
MONGODB_URI="mongodb+srv://<username>:<password>@<cluster>.mongodb.net/bep86?retryWrites=true&w=majority"

# Port cho server local (mặc định 3000)
PORT=3000
